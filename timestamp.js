// provides functions to write the current frame number and info to a file.
// get config

function writeTimestamp(obj) {
  console.log('i wrote a timestamp');
}

module.exports = { writeTimestamp };
